# Observability Studio — Feature Flags
# ─────────────────────────────────────
# Set a flag to false to disable the accelerator's backend APIs
# AND hide its tile in the UI.

observascore: true   # ObservaScore maturity assessment accelerator
obscrawl: true       # ObsCrawl telemetry crawler & Excel exporter
rca_agent: true      # RCA Agent — multi-tool root cause analysis
red_panel_intelligence: true # RED Panel Intelligence dashboard-quality accelerator
observability_gap_map: true # Observability Gap Map service-level blind-spot accelerator
